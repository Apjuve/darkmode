import React from "react";

function Navbar ({currentPage, handlePageChange}) {
    return (
        <nav>
            <div>
                <a href="/">Form</a>
            </div>
        </nav>
    )
}

export default Navbar;